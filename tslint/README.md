# Padrão de configuração do TSLint - Cubos

## Como usar

Execute `yarn add -D @cubos/tslint` e então inclua a seguinte propriedade no seu `tslint.json`:
```json
{
    "extends": "@cubos/tslint"
}
```